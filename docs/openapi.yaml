# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: maco
    description: maco OpenAPI3.0 Document
    version: 160ec4138d54964dd69b9c2f900c1ad7f0465508
paths:
    /v1/call:
        get:
            tags:
                - MacoRPC
            operationId: MacoRPC_Call
            parameters:
                - name: request.id
                  in: query
                  description: 请求 id，确认请求唯一值
                  schema:
                    type: string
                - name: request.selector.minions
                  in: query
                  schema:
                    type: array
                    items:
                        type: string
                - name: request.function
                  in: query
                  description: '请求方法，使用.分割，确认对应模块下的方法，如: a.b.c'
                  schema:
                    type: string
                - name: request.args
                  in: query
                  description: 方法参数
                  schema:
                    type: array
                    items:
                        type: string
                - name: request.timeout
                  in: query
                  description: 请求超时时长
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/rpc.macopb.CallResponse'
    /v1/ping:
        get:
            tags:
                - MacoRPC
            operationId: MacoRPC_Ping
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/rpc.macopb.PingResponse'
components:
    schemas:
        rpc.macopb.CallResponse:
            type: object
            properties:
                report:
                    $ref: '#/components/schemas/types.Report'
        rpc.macopb.PingResponse:
            type: object
            properties:
                message:
                    type: string
        types.Report:
            type: object
            properties:
                items:
                    type: array
                    items:
                        $ref: '#/components/schemas/types.ReportItem'
                summary:
                    $ref: '#/components/schemas/types.ReportSummary'
            description: Report Minion 执行结果
        types.ReportItem:
            type: object
            properties:
                minion:
                    type: string
                startTimestamp:
                    type: string
                endTimestamp:
                    type: string
                result:
                    type: boolean
                error:
                    type: string
                data:
                    type: string
                    format: bytes
        types.ReportSummary:
            type: object
            properties:
                success:
                    type: string
                changes:
                    type: string
                failed:
                    type: string
                total:
                    type: string
tags:
    - name: MacoRPC
